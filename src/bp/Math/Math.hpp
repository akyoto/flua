#include <cmath>

template <typename T>
inline double bp_sin(T radian) {
	return sin(radian);
}

template <typename T>
inline double bp_cos(T radian) {
	return cos(radian);
}

template <typename T>
inline double bp_sqrt(T num) {
	return sqrt(num);
}
