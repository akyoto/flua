Point
	init self.x, self.y
		...
		
	get
		x
			return self.x
			
		y
			return self.y
	
	set
		x value
			self.x = value
		
		y value
			self.y = value
			
	distanceTo point
		return distance(self.x, self.y, point.x, point.y)

# Overloaded functions
print p : Point
	bp_print p.x
	bp_print ", "
	bp_print p.y