Point
	init self.x, self.y
		...
		
	get
		x
			return self.x
			
		y
			return self.y
	
	set
		x value
			self.x = value
		
		y value
			self.y = value
			
	distanceTo point
		return distance(self.x, self.y, point.x, point.y)