import bp.Math
import Point

Circle
	init self.midPoint, self.radius
		...
		
	contains point
		return distanceSqr(self.midPoint.x, self.midPoint.y, point.x, point.y) <= (self.radius * self.radius)
		
	diameter
		return self.radius * 2
		
	intersects circle
		distance = distanceSqr(self.midPoint.x, self.midPoint.y, circle.midPoint.x, circle.midPoint.y)
		sumRadi = self.radius + circle.radius
		return d < (sumRadi * sumRadi)
