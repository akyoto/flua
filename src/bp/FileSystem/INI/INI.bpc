import FileSystem.*
import Container.Map.*

Category
	
	init self.name
		self.settings = Map(String, String)
		
	[key]
		return self.settings[key]
	
INIFile = FileLoader
	
	init self.fileName
		self.categories = [Category]
		
	load
		in File(self.fileName) as file
			while not file.eof
				# TODO: Load INI
				print file.readLine
				
